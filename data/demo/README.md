# Demo data

`rows-demo.tsv` contains synthetic x402-style resource rows used by `npm run seed:demo`.

The file is intentionally small and safe to publish. It demonstrates the expected shape of discovery data without exposing production datasets or private infrastructure.
